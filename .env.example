# Contraseña para /test/ (nunca se sube a GitHub).
# En Vercel: Project → Settings → Environment Variables → TEST_PASSWORD
# En local: usar "vercel dev" para que la API lea esta variable.
TEST_PASSWORD=
