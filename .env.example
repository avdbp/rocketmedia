# Copiar a .env y rellenar.
# En Vercel: Project → Settings → Environment Variables → añadir TEST_PASSWORD
# Si ejecutas "npm run build" en local, la contraseña se escribe en test/index.html;
# antes de hacer commit restaura: git checkout test/index.html
TEST_PASSWORD=
